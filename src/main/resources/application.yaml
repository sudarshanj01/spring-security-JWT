spring:
  application:
    name: security
  datasource:
    url: jdbc:mysql://localhost:3306/javaa
    username: root
    password: root
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
